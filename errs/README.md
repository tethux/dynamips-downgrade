# Errors package

`errs` defines the error categories and typed operation errors returned by the
Go binding. Import it as `github.com/tethux/dynamips-downgrade/errs`.

Go reference: [errs](https://pkg.go.dev/github.com/tethux/dynamips-downgrade/errs).
